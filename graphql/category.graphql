query category($uri: [String]!, $siteId: [QueryArgument]!) {
  category(uri: $uri, siteId: $siteId) {
    id
    title
    groupHandle
    siteId
    level
    localized {
      siteId
      uri
      language
    }
  }
}
